spring:
  profiles:
    active: region1
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      uri: http://localhost:8889
