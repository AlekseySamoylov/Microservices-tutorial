spring:
  profiles:
    active: ${region:region1}
  application:
    name: service-discovery-client
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      uri: http://localhost:8889
