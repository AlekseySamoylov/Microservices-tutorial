spring:
  profiles:
    active: ${region:region1}
  application:
    name: service-discovery-client
  cloud:
    config:
      uri: http://localhost:8889
logging:
  config: http://127.0.0.1:8889/logging/production/master/client-logback-spring.xml
