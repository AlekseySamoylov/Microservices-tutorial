server:
  port: ${PORT:8889}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AlekseySamoylov/microservice-config.git

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
